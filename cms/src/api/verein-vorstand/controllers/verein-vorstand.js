'use strict';

/**
 * verein-vorstand controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::verein-vorstand.verein-vorstand');
